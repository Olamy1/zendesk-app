import React from "react";
export default function Settings() {
  return (
    <div className="space-y-2">
      <h2 className="text-lg font-semibold">Settings</h2>
      <p className="text-sm text-gray-600">For MVP, app settings are managed via environment variables and Zendesk app parameters.</p>
    </div>
  );
}